#version 410 core

void main() {
    //calculate normal
    // vec3 normal;
    // normal.xy = gl_PointCoord * 2.0 - 1.0;
    // float r2 = dot(normal.xy, normal.xy);

    // if (r2 > 1.0) {
    //     discard;
    // }

    gl_FragColor = vec4(1);
}
